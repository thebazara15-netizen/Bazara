import Link from "next/link";import{StatusBadge,formatDate}from"../rfq/RfqUi";const supplier=vendor=>vendor?.companyName||[vendor?.firstName,vendor?.lastName].filter(Boolean).join(" ")||"Marketplace supplier";
export default function AccountInquiries({inquiries,onOpenInbox}){return <section><p className="marketplace-eyebrow">Supplier conversations</p><h2 className="mt-2 text-3xl font-bold">My Inquiries</h2><div className="mt-7 space-y-4">{inquiries.length?inquiries.map(item=><article key={item.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><h3 className="font-bold">{item.product?.name||"Product inquiry"}</h3><p className="mt-1 text-sm text-slate-600">{supplier(item.vendor)} · Quantity {item.quantity} · {formatDate(item.createdAt)||"—"}</p></div><StatusBadge status={item.status}/></div><p className="mt-4 whitespace-pre-wrap text-sm leading-6 text-slate-600">{item.message||"No message provided."}</p><div className="mt-4 flex flex-wrap gap-3"><button type="button" onClick={()=>onOpenInbox(item.id)} className="marketplace-button-primary">Open in Inbox</button>{item.productId&&<Link href={`/product/${item.productId}`} className="marketplace-button-secondary">Open product</Link>}</div></article>):<div className="rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center"><h3 className="font-bold">No inquiries yet</h3><Link href="/products" className="marketplace-button-primary mt-5">Browse Products</Link></div>}</div></section>;}
