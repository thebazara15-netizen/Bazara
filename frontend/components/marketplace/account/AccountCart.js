import Link from "next/link";
export default function AccountCart({ items }) { const quantity=items.reduce((sum,item)=>sum+Number(item.quantity||0),0); return <section><p className="marketplace-eyebrow">Purchase planning</p><h2 className="mt-2 text-3xl font-bold">Cart</h2><div className="mt-7 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-sm text-slate-500">Current cart quantity</p><p className="mt-2 text-4xl font-bold">{quantity}</p><p className="mt-3 text-sm text-slate-600">{items.length ? `${items.length} distinct product${items.length === 1 ? "" : "s"} in your cart.` : "Your cart is currently empty."}</p><Link href={items.length ? "/cart" : "/products"} className="marketplace-button-primary mt-6">{items.length ? "View Cart" : "Browse Products"}</Link></div></section>; }
