"use client";

import { useState } from "react";
const fields = [["firstName","First name"],["lastName","Last name"],["companyName","Company name"],["phone","Phone"],["location","Location"],["businessType","Business type"]];
const input = "mt-2 min-h-12 w-full rounded-xl border border-slate-300 px-3.5 text-sm outline-none focus:border-orange-600 focus:ring-2 focus:ring-orange-100";
export default function AccountProfile({ profile, saving, onSave }) { const [form,setForm]=useState(() => Object.fromEntries([...fields.map(([key]) => [key, profile?.[key] || ""]),["aboutCompany",profile?.aboutCompany || ""]])); return <section><p className="marketplace-eyebrow">Buyer profile</p><h2 className="mt-2 text-3xl font-bold">Profile</h2><form onSubmit={(event) => { event.preventDefault(); onSave(form); }} className="mt-7 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold sm:col-span-2">Email<input value={profile?.email || ""} readOnly className={`${input} bg-slate-100 text-slate-500`}/><span className="mt-1 block text-xs font-normal text-slate-500">Email changes require a separate verification workflow.</span></label>{fields.map(([key,label]) => <label key={key} className="text-sm font-semibold">{label}<input maxLength={key === "phone" ? 30 : key === "companyName" || key === "location" ? 200 : 150} value={form[key]} onChange={(event) => setForm((current) => ({...current,[key]:event.target.value}))} className={input}/></label>)}<label className="text-sm font-semibold sm:col-span-2">Business description<textarea rows={5} maxLength={2000} value={form.aboutCompany} onChange={(event) => setForm((current) => ({...current,aboutCompany:event.target.value}))} className={`${input} py-3`}/></label></div><div className="mt-6 flex justify-end"><button disabled={saving} className="marketplace-button-primary disabled:opacity-60">{saving ? "Saving…" : "Save profile"}</button></div></form></section>; }
