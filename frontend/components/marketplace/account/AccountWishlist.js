"use client";
import Link from "next/link";
import ProductGrid from "../ProductGrid";
export default function AccountWishlist({ items, cartProducts, onAddToCart }) { const products=items.map((item)=>item.product).filter(Boolean); return <section><p className="marketplace-eyebrow">Saved catalog</p><h2 className="mt-2 text-3xl font-bold">Saved Products</h2><p className="mt-3 text-sm text-slate-600">Keep useful supplier listings together for later comparison.</p><div className="mt-7">{products.length ? <ProductGrid products={products} loading={false} error={false} searchQuery="" viewerRole="CLIENT" cartProducts={cartProducts} onAddToCart={onAddToCart} onClear={() => {}}/> : <div className="rounded-2xl border border-dashed border-slate-300 bg-white px-5 py-14 text-center"><h3 className="text-lg font-bold">No saved products yet</h3><p className="mt-2 text-sm text-slate-600">Save products from marketplace cards or product detail pages.</p><Link href="/products" className="marketplace-button-primary mt-5">Browse Products</Link></div>}</div></section>; }
