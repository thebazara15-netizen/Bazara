import { displayName, EmptyState, formatDate, formatMoney, SectionTitle, StatusBadge } from "./AdminUi";
export default function AdminOrders({ orders }) { return <section><SectionTitle eyebrow="Order records" title="Orders" description="Read-only presentation of existing order fields; no payment or fulfillment actions."/>{!orders.length?<div className="mt-6"><EmptyState title="No orders" description="Existing marketplace orders will appear here."/></div>:<div className="mt-6 overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table className="min-w-[680px] w-full text-left text-sm"><thead className="bg-slate-50"><tr>{["Order ID","Buyer","Total","Status","Created"].map((label)=><th key={label} scope="col" className="px-4 py-3 font-bold text-slate-600">{label}</th>)}</tr></thead><tbody className="divide-y divide-slate-200">{orders.map((order)=><tr key={order.id}><td className="px-4 py-4 font-bold">#{order.id}</td><td className="px-4 py-4">{displayName(order.buyer)}</td><td className="px-4 py-4">{formatMoney(order.totalAmount)}</td><td className="px-4 py-4"><StatusBadge value={order.status}/></td><td className="px-4 py-4 whitespace-nowrap">{formatDate(order.createdAt)}</td></tr>)}</tbody></table></div>}</section>; }
