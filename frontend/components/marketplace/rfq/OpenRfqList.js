"use client";

import QuoteForm from "./QuoteForm";
import { formatDate, formatMoney, StatusBadge } from "./RfqUi";

export default function OpenRfqList({ rfqs, isVendor, drafts, onDraftChange, onQuote, submittingId, submittedRfqIds = new Set() }) {
  return <div className="grid gap-4 lg:grid-cols-2">{rfqs.map((rfq) => <article key={rfq.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-xs font-bold uppercase tracking-wider text-orange-700">{rfq.category || "General requirement"}</p><h3 className="mt-2 break-words text-lg font-bold text-slate-950">{rfq.title}</h3></div><StatusBadge status={rfq.status}/></div>{rfq.description && <p className="mt-3 line-clamp-3 whitespace-pre-wrap text-sm leading-6 text-slate-600">{rfq.description}</p>}<dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><div className="rounded-xl bg-slate-50 p-3"><dt className="text-xs text-slate-500">Quantity</dt><dd className="mt-1 font-bold text-slate-950">{rfq.quantity} {rfq.unit || "units"}</dd></div><div className="rounded-xl bg-slate-50 p-3"><dt className="text-xs text-slate-500">Budget</dt><dd className="mt-1 font-bold text-slate-950">{formatMoney(rfq.budget)}</dd></div></dl><div className="mt-3 flex flex-wrap gap-x-4 gap-y-1 text-xs text-slate-500">{rfq.deliveryLocation && <span>{rfq.deliveryLocation}</span>}{formatDate(rfq.createdAt) && <span>Posted {formatDate(rfq.createdAt)}</span>}</div>{isVendor && (submittedRfqIds.has(rfq.id) ? <p className="mt-4 rounded-lg bg-sky-50 px-3 py-2 text-sm font-semibold text-sky-800">Quotation already submitted</p> : <QuoteForm value={drafts[rfq.id] || {}} onChange={(value) => onDraftChange(rfq.id, value)} onSubmit={(event) => { event.preventDefault(); onQuote(rfq.id); }} submitting={submittingId === rfq.id}/>)}</article>)}</div>;
}
